{
  "name": "flowbit-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:ai": "cd services/vanna && python main.py",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "install:all": "npm install && npm install --workspace=apps/web && npm install --workspace=apps/api",
    "db:setup": "npm run db:generate --workspace=apps/api && npm run db:migrate --workspace=apps/api && npm run db:seed --workspace=apps/api",
    "setup": "npm run install:all && npm run db:setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}